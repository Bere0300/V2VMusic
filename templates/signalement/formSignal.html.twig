{% extends 'base.html.twig' %}

{% block css %}
    {{ parent() }}
    <link rel="stylesheet" href="{{asset('CSS/form.css')}}">
{% endblock %}

{% block title %} Signaler {% endblock %}

{% block body %}


<div class='main'>

            {{ form_start(formSignal) }}
                <h1>Signalement</h1>
                    {{ form_row(formSignal.email) }}
                    {{ form_row(formSignal.sujet) }}
                    {{ form_row(formSignal.contenu) }}

                <button type="submit" class="button">Valider</button>
                
            {{ form_end(formSignal) }}
</div>
{% endblock %}